import { customizer } from './admin/customizer';

/**
 * Main customizer file.
 *
 * @author Davey Jacobson <davey.jacobson@tribusgroup.com>
 */
( function( $ ) {
	$( window ).on( 'load', customizer );
} )( jQuery );
